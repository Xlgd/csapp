char            int             movsbl (%dil), %eax      movw %eax, %si
char            unsigned        movzbl (%dil), %eax      movw %eax, %si
unsigned char   long            movsbq (%dil), %rax     movq %rax, %rsi
int             char            movb   (%di), %al       movb %al, %sil
unsigned        unsigned char   movb   (%di), %al       movb %al, %sil
char            short           movsbw (%dil), %ax      movw %ax, %si