A. 将%rdx置为0，运用了对于任意的x，x ^ x = 0这一属性。
B. movq $0 %rdx。
C. 汇编和反汇编这段代码，我们发现使用xorq的版本只需要3个字节，
   而使用movq的版本需要7个字节。
   其他将%rdx设置为0的方法都依赖这样一个属性：即任何更新低位4字节的指令都会把高位字节设置为0，
   因此，我们可以使用xorl %edx, %edx 或 movl $0, %edx。
