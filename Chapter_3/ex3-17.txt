A.
long goto2diff_se(long x, long y) {
    long result;
    if (x < y) {
        goto x_l_y;
    }
    ge_cnt++;
    result = x - y;
    return result;

x_l_y:
    lt_cnt++;
    result = y - x;
    return result;
}

B. 要看判断条件为真或为假的概率大小，如果为真的概率大则使用原文中的形式，反之使用此形式