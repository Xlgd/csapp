A. 用高位做索引，每个连续的数组片由2 ^ t个块组成，这里t是标记位数。因此，数组头
   2 ^ t个连续的块都会映射到组0，接下来的2 ^ t个块会映射到组1，以此类推。
B. 由提供信息可得，每个高速缓存行中有18个标记位。因此，数组中头2 ^ 18个块会映射到组0，
   因为我们数组只由(4096 * 4) / 32 = 512个块组成，所以数组中所有的块都被映射到组0，导致
   任何时刻，高速缓存至多只能保存一个数组块。